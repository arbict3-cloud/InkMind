# TODO

## 🔴 高优先

- **长篇上下文**：自动维护故事纲要、人物关系、伏笔、时间线，并注入每次 LLM 调用。
- **一致性校验**：生成后检查人物、时间线、伏笔矛盾，给出可执行修订建议。
- **持久化任务队列**：后台/Agent 任务入库，支持重启恢复、取消、分页查询和事件追踪。
- **SSE 重连完善**：补齐 `last_event_id`、会话快照、重连去重和断线恢复。
- **数据库迁移**：引入 Alembic，替代 `main.py` 中的 SQLite 手写迁移和静默失败逻辑。
- **测试与 CI**：补后端 pytest/ruff、前端 typecheck/eslint/vitest，并在 PR 中自动运行。

## 🟡 中优先

- **流式 Markdown 增量渲染**：替换 `react-markdown` 全量重渲染，降低长回复卡顿。
- **ContentBlock 内联渲染**：工具调用、任务进度、结果、提问统一进入消息流渲染。
- **多会话管理**：支持创作方向并行对话、会话列表、切换、删除和历史恢复。
- **斜杠命令**：提供 `/续写`、`/改写`、`/生成大纲`、`/质量检查` 等快捷触发。
- **多步问答向导**：AskUserQuestion 多问题分步展示，支持返回、补充和确认。
- **导入/导出增强**：补 EPUB/DOCX 导出，支持 TXT/Markdown/DOCX 导入和整本作品归档。
- **写作 Copilot**：实时续写建议、主动卡文检测、参考文本风格模仿。
- **前端模块拆分**：拆分 `NovelWrite`、`AiAssistantFloating`、`api/client` 等大文件。

## 🟢 低优先

- **Sepia 主题**：补全主题类型、Ant Design 配置和切换入口。
- **章节大纲管理**：思维导图式大纲、拖拽排序、章节状态标记。
- **质量评估闭环**：低分自动重试或生成修订方案，而不只是展示评分。
- **拼写与敏感词检查**：错别字、标点、敏感词在生成后/导出前扫描。
- **写入权限分层**：保存、删除、覆盖等高风险操作增加校验与确认。
- **协作编辑**：多用户协作、权限管理和变更记录。

## ⚙️ 工程

- **PostgreSQL 支持**：生产环境使用 PostgreSQL，保留 SQLite 作为本地默认。
- **Redis/缓存**：缓存摘要、评估、模型元数据等可复用结果，减少 token 消耗。
- **数据库优化**：索引、分页、预加载、连接池和慢查询排查。
- **安全加固**：API Key 加密/脱敏、密码强度、刷新 Token、速率限制、CSRF/HTTPS。
- **可观测性**：Agent 决策、LLM 调用、任务耗时、token/费用链路可追踪。
- **数据备份**：定期备份、恢复验证、作品级导入导出。
- **i18n 拆分**：按语言和业务域拆分翻译文件，减少单文件维护成本。
- **仓库清洁**：确保 `.venv`、`node_modules`、`dist`、数据库、备份文件不进入版本库。
